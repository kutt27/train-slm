# Train SLM

The entire idea of this experiment revolve around this idea:
> How did the industry evolve from statistical language models to modern LLM systems?

The other constraint is I will be using the best performing methods for training. Rather than using a char level tokenizer, I will use BPE. Moving forward, will be using LoRA for training. SFT for fine tuning rather than CFT. 

Insertion of Mojo for tokenizer is still in doubt, each one working on top of each other is a brief idea, therefore no chance and rather sticking with python.


